import { AddNewExpenseForm } from "@/components/financeTracker/AddNewExpenseForm";

const AddNewExpensePage = () => {
  return (
    <main className="flex flex-col items-center mt-4 min-h-screen">
      <AddNewExpenseForm />
    </main>
  );
};

export default AddNewExpensePage;
